# Suppress experimental warnings from npm's internal modules
# Note: This warning comes from npm's internal dependencies and is harmless
# If .npmrc doesn't work, use NODE_OPTIONS environment variable instead

